/*import org.openqa.selenium.By;

import org.openqa.selenium.By;
import org.openqa.selenium.WebDriver;

public class InterviewSimulation {

    private WebDriver driver;
    private By astronautName = By.id(PLACEHOLDER);
    private By planetDestination = By.id("PLACEHOLDER");
    private By launchButton = By.id();

    public InterviewSimulation(WebDriver driver){
        this.driver = driver;
    }

    public void setAstronautName (String name){
        driver.findElement(astronautName).sendKeys(name);
    }
    public void setPlanetDestination (String plantName){
        driver.findElement(planetDestination).sendKeys(plantName);
    }
    public LaunchPage clickLaunchButton (){
        driver.findElement(launchButton).click();
        return new LaunchPage(driver);
    }
}*/



